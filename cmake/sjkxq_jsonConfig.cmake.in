@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# 添加依赖项检查（如果有的话）
# find_dependency(依赖项名称 版本要求)

# 导入目标
include("${CMAKE_CURRENT_LIST_DIR}/sjkxq_jsonTargets.cmake")

# 检查所有必需的组件是否都已找到
check_required_components(sjkxq_json)